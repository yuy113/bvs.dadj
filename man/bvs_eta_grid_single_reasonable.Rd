% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bvs_eta_tuning.R
\name{bvs_eta_grid_single_reasonable}
\alias{bvs_eta_grid_single_reasonable}
\title{Build Reasonable Single-Network Eta/Moller Grid}
\usage{
bvs_eta_grid_single_reasonable(
  eta1_sd = NULL,
  adj = NULL,
  eta1_frac = seq(0.2, 1.2, by = 0.1),
  mu = stats::qlogis(0.02),
  phase_min_eta = 0,
  phase_max_eta = 1.5,
  phase_step_size = 0.05,
  phase_num_rep = 8L,
  phase_Tmax = 64L,
  eta_floor = 1e-04,
  eta_cap = 2,
  mu_tilde = c(stats::qlogis(0.01), stats::qlogis(0.02), stats::qlogis(0.05)),
  eta1_tilde_frac = c(0.2, 0.35, 0.5),
  e_eta = c(1, 2),
  f_eta = c(1, 2)
)
}
\arguments{
\item{eta1_sd}{Optional numeric vector of candidate \code{eta1_sd}.}

\item{adj}{Optional adjacency matrix for phase-based grid construction.}

\item{eta1_frac}{Fractions of phase-transition anchor for constructing
\code{eta1_sd} when \code{eta1_sd} is NULL.}

\item{mu}{Ising external field used for phase profiling.}

\item{phase_min_eta}{Minimum eta used for phase profiling.}

\item{phase_max_eta}{Maximum eta used for phase profiling.}

\item{phase_step_size}{Eta step size used for phase profiling.}

\item{phase_num_rep}{Replications used for phase profiling.}

\item{phase_Tmax}{Propp-Wilson horizon used for phase profiling.}

\item{eta_floor}{Minimum allowed eta upper bound.}

\item{eta_cap}{Maximum allowed eta upper bound.}

\item{mu_tilde}{Candidate vector for Moller auxiliary \code{mu_tilde}.}

\item{eta1_tilde_frac}{Fractions used to derive \code{eta1_tilde} from
\code{eta1_sd}.}

\item{e_eta}{Candidate vector for Moller Beta prior shape \code{a}.}

\item{f_eta}{Candidate vector for Moller Beta prior shape \code{b}.}
}
\value{
A data.frame with columns
  \code{eta1_sd, mu_tilde, eta1_tilde, e_eta, f_eta}.
  If phase profiling is used, a \code{"phase_profile"} attribute is attached.
}
\description{
Convenience constructor for single-adjacency tuning grids that couples
\code{eta1_tilde} to \code{eta1_sd} through scale factors
(\code{eta1_tilde = eta1_sd * eta1_tilde_frac}).
}
\details{
This is useful for phase-transition-guided tuning where Moller auxiliary
interaction should remain proportional to the candidate Ising upper bound.
}
