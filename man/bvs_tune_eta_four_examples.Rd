% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bvs_eta_tuning.R
\name{bvs_tune_eta_four_examples}
\alias{bvs_tune_eta_four_examples}
\title{Tune Eta for Four Standard Scenarios (Sim1/Sim2/Real1/Real2)}
\usage{
bvs_tune_eta_four_examples(
  sim1,
  sim2,
  real1,
  real2,
  pip_threshold_fixed = 0.5,
  target_fdr = 0.05,
  require_truth = TRUE,
  verbose = TRUE,
  ...
)
}
\arguments{
\item{sim1, sim2, real1, real2}{Scenario argument lists. Each list should contain
the scenario-specific inputs required by \code{\link{bvs_tune_eta}}
(for example \code{X}, \code{y}, \code{adj_type}, adjacency inputs, and
optional \code{truth_idx}).}

\item{pip_threshold_fixed}{Fixed PIP threshold used for all scenarios.}

\item{target_fdr}{Target upper bound for FDR.}

\item{require_truth}{Logical; if TRUE, each scenario must include non-empty
\code{truth_idx} for supervised FDR/sensitivity optimization.}

\item{verbose}{Logical; print progress across scenarios.}

\item{...}{Common arguments passed to every scenario (for example
\code{sampler}, \code{niter}, \code{burnin}, \code{thin}, and other MCMC
hyperparameters).}
}
\value{
A list with:
\describe{
  \item{scenario_results}{Named list of \code{"bvs_eta_tune"} objects.}
  \item{best_table}{One-row-per-scenario summary of selected candidates.}
}
}
\description{
Convenience batch runner over four scenarios using a constant PIP threshold
(default 0.5) and shared FDR target (default 0.05).
}
\details{
This helper is designed for the common workflow of two simulation studies
and two real-data applications.
}
