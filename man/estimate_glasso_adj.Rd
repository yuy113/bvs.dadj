% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helpers.R
\name{estimate_glasso_adj}
\alias{estimate_glasso_adj}
\title{Estimate Adjacency Matrix via Graphical Lasso}
\usage{
estimate_glasso_adj(
  X,
  criterion = c("ebic", "ric"),
  nlambda = 30,
  lambda.min.ratio = 0.01,
  symmetrize = TRUE
)
}
\arguments{
\item{X}{An \code{n x p} design matrix.}

\item{criterion}{Model selection criterion: \code{"ebic"} (extended BIC,
default) or \code{"ric"} (rotation information criterion).}

\item{nlambda}{Number of lambda values for the regularization path
(default 30).}

\item{lambda.min.ratio}{Ratio of smallest to largest lambda (default 0.01).}

\item{symmetrize}{Logical; symmetrize the adjacency and keep upper
triangle only (default TRUE).}
}
\value{
A \code{p x p} integer adjacency matrix (0/1), symmetric with
  zero diagonal.
}
\description{
Uses the \pkg{huge} package to estimate a sparse precision matrix via the
graphical lasso algorithm and extract the binary adjacency matrix.
}
\details{
Requires the \pkg{huge} package to be installed.  The function
  runs \code{huge()} with \code{method = "glasso"} and selects the optimal
  model via \code{huge.select()} with the specified criterion.
}
\examples{
\dontrun{
set.seed(42)
n <- 200
p <- 50
X <- matrix(rnorm(n * p), n, p)
adj <- estimate_glasso_adj(X, criterion = "ebic")
sum(adj) # number of detected edges
}

}
