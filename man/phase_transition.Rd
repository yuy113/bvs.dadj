% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/phase_transition.R
\name{phase_transition}
\alias{phase_transition}
\title{Phase Transition Detection}
\usage{
phase_transition(
  adj,
  adj2 = NULL,
  mu = -log(1/0.3 - 1),
  min_eta = 0,
  max_eta = 2,
  step_size = 0.01,
  num_rep = 10L,
  Tmax = 64L
)
}
\arguments{
\item{adj}{A \code{p x p} binary adjacency matrix.}

\item{adj2}{Optional second adjacency matrix (for dual-eta phase transition).}

\item{mu}{Ising external field (default \code{-log(1/0.3-1)}).}

\item{min_eta}{Minimum eta value (default 0).}

\item{max_eta}{Maximum eta value (default 2).}

\item{step_size}{Step size for eta grid (default 0.01).}

\item{num_rep}{Number of Propp-Wilson replications per eta (default 10).}

\item{Tmax}{Maximum Propp-Wilson doubling time (default 64).}
}
\value{
An integer matrix of model sizes (one row per eta value,
  one column per replication).
}
\description{
Sweeps the Ising coupling parameter eta across a range and generates gamma
vectors via Propp-Wilson perfect simulation at each value to visualize the
phase transition in model size.
}
\details{
For dual-eta mode (when \code{adj2} is supplied), both eta1 and eta2 are
swept jointly.  The phase transition point indicates where the Ising prior
transitions from sparse to dense model selection.
}
\examples{
\dontrun{
p <- 50
R <- diag(0, p)
R[1:10, 1:10] <- 1; diag(R) <- 0
pt <- phase_transition(R, max_eta = 1.5)
matplot(pt, type = "l", xlab = "eta index", ylab = "model size")
}

}
